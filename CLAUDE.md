# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Status

This project is in its initial state. The only content is an empty `plugin/` directory and `.claude/settings.json` enabling the `superpowers@claude-plugins-official` plugin.

No build system, package manager, or source code has been added yet.
