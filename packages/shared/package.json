{
  "name": "@riftview/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Platform-agnostic analysis, graph, drift, and scan primitives shared between @riftview/desktop and @riftview/cli",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./snapshot": "./src/snapshot/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "ini": "^6.0.0"
  },
  "devDependencies": {
    "@types/ini": "^4.1.1"
  }
}
