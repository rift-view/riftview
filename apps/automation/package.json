{
  "name": "@riftview/automation",
  "version": "0.1.0",
  "private": true,
  "description": "Local CLI + skill bootstrap for the Linear → PR automation.",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@riftview/automation-core": "*",
    "vitest": "^4.0.18"
  }
}
